{
  "name": "distribution-fixture-serverless-pricing-api",
  "version": "1.0.0",
  "private": true,
  "description": "Small production API fixture for distribution testing against serverless HTTP functions.",
  "type": "commonjs",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test"
  }
}
